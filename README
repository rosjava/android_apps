To configure (only need to do this the first time):

$ make conf

To build:

$ make

You should find .apk files in the bin subdirectory of each project.  To upload them to a connected device:

$ make load

